* Current day is displayed

* filtering the time of day:
    - get current time,
    - if time > time block:
        - red
    - else if time == time block:
        - yellow
    - else:
        - green

* Clickable time blocks to enter events

* Save button for time block:
    - the time block maintains its event